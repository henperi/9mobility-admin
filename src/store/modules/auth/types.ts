const SET_AUTH_USER = 'auth/SET_AUTH';
const SET_AUTH_TOKEN = 'auth/SET_AUTH_TOKEN';
const REMOVE_AUTH_USER = 'auth/REMOVE_AUTH_USER';

export const types = {
  SET_AUTH_USER,
  SET_AUTH_TOKEN,
  REMOVE_AUTH_USER,
};
