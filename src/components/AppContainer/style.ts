import styled from 'styled-components';

const AppContainer = styled.div`
  width: 100%;
  height: 100vh;
  background-color: #f5f5f5;
  overflow-x: hidden;
  position: relative;
`;

export const Styles = {
  AppContainer,
};
